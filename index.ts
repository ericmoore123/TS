// TypeScript

const birthdayGreeter = (name: string, age: number): string => {
    return `Happy Birthday ${name}, you are now ${age} years old!`;
};

const user = 'John Doe';
const age = 22; 
console.log(birthdayGreeter(user, age));



